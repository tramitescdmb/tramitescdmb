# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files (never commit real secrets)
.env
.env.local
.env.*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*

# fuentes originales de los procedimientos (pesan varios MB cada una, no hace falta versionarlas)
/procedimientos/

# PDF fuente del modelo MOREQ/TRD/CCD usados para la matriz de cumplimiento y el import de TRD
# (30MB+, ya extraídos/procesados; no hace falta versionarlos)
/Modelo/

# Scripts temporales de pruebas de sesión (no versionar)
_tmp-*

# Vercel CLI (vercel link)
.vercel
